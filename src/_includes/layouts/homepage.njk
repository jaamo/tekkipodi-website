<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
    {% if description %}
    <meta name="description" content="{{ description }}">
    {% endif %}
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <link rel="apple-touch-icon" href="/images/favicon.png">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="{% if title %}{{ title }} | {% endif %}{{ site.title }}">
    <meta property="og:description" content="{% if description %}{{ description }}{% else %}{{ site.tagline }}{% endif %}">
    <meta property="og:image" content="/images/cover.jpg">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{% if title %}{{ title }} | {% endif %}{{ site.title }}">
    <meta name="twitter:description" content="{% if description %}{{ description }}{% else %}{{ site.tagline }}{% endif %}">
    <meta name="twitter:image" content="/images/cover.jpg">

    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/vendor/cookieconsent.css">

    <!-- Organization Schema -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "Tekkipodi",
      "url": "https://www.tekkipodi.fi",
      "logo": "https://www.tekkipodi.fi/images/tekkipodi-logo.png",
      "description": "{{ site.tagline }}",
      "sameAs": [
        "{{ site.podcastLinks.spotify }}",
        "{{ site.podcastLinks.apple }}",
        "{{ site.podcastLinks.youtube }}"
      ],
      "areaServed": "FI",
      "inLanguage": "fi"
    }
    </script>

    <!-- Podcast Schema -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "PodcastSeries",
      "name": "Tekkipodi",
      "description": "{{ site.tagline }}",
      "url": "https://www.tekkipodi.fi",
      "image": "https://www.tekkipodi.fi/images/cover.jpg",
      "author": {
        "@type": "Organization",
        "name": "Tekkipodi"
      },
      "publisher": {
        "@type": "Organization",
        "name": "Tekkipodi"
      },
      "inLanguage": "fi",
      "genre": ["Technology"],
      "webFeed": "{{ site.podcastLinks.rss }}"
    }
    </script>

    <!-- Google Analytics (loads only when analytics cookies accepted) -->
    <script type="text/plain" data-category="analytics" src="https://www.googletagmanager.com/gtag/js?id=G-8CRQ3XK9BP"></script>
    <script type="text/plain" data-category="analytics">
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-8CRQ3XK9BP');
    </script>
</head>
<body>

    <section class="hero" id="hero-section">
        <div class="hero-content">
            <img src="{{ site.logo }}" alt="{{ site.title }} Logo" class="hero-logo">
            <p class="hero-tagline">{{ site.tagline }}</p>
            <div class="platforms platforms--black">
                <a href="{{ site.podcastLinks.spotify }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on Spotify">
                    <img src="/images/logo-spotify.svg" alt="Spotify">
                </a>
                <a href="{{ site.podcastLinks.apple }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on Apple Podcasts">
                    <img src="/images/icon-apple.svg" alt="Apple Podcasts">
                </a>
                <a href="{{ site.podcastLinks.youtube }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on YouTube">
                    <img src="/images/logo-youtubemusic.svg" alt="YouTube Music">
                </a>
                <a href="{{ site.podcastLinks.rss }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="RSS Feed">
                    <img src="/images/icon-rss.svg" alt="RSS Feed">
                </a>
            </div>
        </div>
    </section>

    <main>
        <div class="slice slice--black slice--bottom-decoration slice--top-decoration">
            <div class="container">
                <h2>Uusimmat jaksot</h2>

                <section class="episodes-list">
                    <div class="episodes-grid">
                        {% for post in collections.blog %}
                        <article class="episode-card">
                            <a href="{{ post.url }}" class="episode-card-thumbnail">
                                <img src="/images/cover.jpg" alt="{{ post.data.title }}" class="episode-thumbnail-img">
                            </a>
                            <div class="episode-card-body">
                                <h3 class="episode-card-title">
                                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                                </h3>
                                <div class="episode-card-meta">
                                    {% if post.data.date %}
                                    <span class="episode-card-date">
                                        <svg viewBox="0 0 24 24" fill="currentColor" width="16" height="16">
                                            <path d="M19 3h-1V1h-2v2H8V1H6v2H5c-1.11 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V8h14v11zM7 10h5v5H7z"/>
                                        </svg>
                                        {{ post.data.date | dateFormat("MMM d, yyyy") }}
                                    </span>
                                    {% endif %}
                                    {% if post.data.length %}
                                    <span class="episode-card-length">
                                        <svg viewBox="0 0 24 24" fill="currentColor" width="16" height="16">
                                            <path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z"/>
                                        </svg>
                                        {{ post.data.length }}
                                    </span>
                                    {% endif %}
                                </div>
                                {% if post.data.description %}
                                <p class="episode-card-description">{{ post.data.description }}</p>
                                {% endif %}
                            <a href="{{ post.url }}" class="button">
                                Lue lisää
                            </a>
                            </div>
                        </article>
                        {% endfor %}
                    </div>
                </section>

            </div>

        </div>

        <div class="slice slice--white">
            <div class="container pt-xl pb-l">
                <div id="guestbook-widget" data-disable-episode="true"></div>
                <script src="{{ env.guestbookWidgetUrl }}"></script>
            </div>
        </div>

        <div class="slice slice--black slice--bottom-decoration slice--top-decoration">
            <div class="container pt-xl pb-xl">
                <blockquote class="quote-text" id="random-quote" data-quotes='["Korkealaatuista kamaa!", "Tykkäsin sun henkilökohtaisesta otteesta.", "Kuuntelin just kaikki jaksot ja koukutuin täysin."]'>
                    <!-- Quote will be inserted by JavaScript -->
                </blockquote>
                <script>
                    (function() {
                        const quoteElement = document.getElementById('random-quote');
                        if (quoteElement) {
                            try {
                                const quotes = JSON.parse(quoteElement.getAttribute('data-quotes'));
                                const randomIndex = Math.floor(Math.random() * quotes.length);
                                quoteElement.textContent = '"' + quotes[randomIndex] + '"';
                            } catch (e) {
                                console.error('Error parsing quotes:', e);
                            }
                        }
                    })();
                </script>
            </div>
        </div>

        <div class="slice slice--white">
            <div class="container pt-xl pb-xl">
                <div class="content">
                    {{ content | safe }}
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="slice slice--black slice--top-decoration">
            <div class="container pt-m pb-l">
                <div class="platforms platforms--white">
                    <a href="{{ site.podcastLinks.spotify }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on Spotify">
                        <img src="/images/logo-spotify.svg" alt="Spotify">
                    </a>
                    <a href="{{ site.podcastLinks.apple }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on Apple Podcasts">
                        <img src="/images/icon-apple.svg" alt="Apple Podcasts">
                    </a>
                    <a href="{{ site.podcastLinks.youtube }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="Listen on YouTube">
                        <img src="/images/logo-youtubemusic.svg" alt="YouTube Music">
                    </a>
                    <a href="{{ site.podcastLinks.rss }}" target="_blank" rel="noopener noreferrer" class="platform-icon" aria-label="RSS Feed">
                        <img src="/images/icon-rss.svg" alt="RSS Feed">
                    </a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        (function() {
            const hero = document.getElementById('hero-section');
            if (!hero) return;

            let ticking = false;

            function updateParallax() {
                const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
                const heroHeight = hero.offsetHeight;
                const heroTop = hero.offsetTop;

                // Only apply parallax when hero is in viewport
                if (scrollTop < heroTop + heroHeight) {
                    // Parallax speed factor (0.5 means background scrolls at 50% speed)
                    const parallaxSpeed = 0.5;
                    const yPos = scrollTop * parallaxSpeed;

                    hero.style.backgroundPosition = `right ${yPos}px`;
                }

                ticking = false;
            }

            function requestTick() {
                if (!ticking) {
                    window.requestAnimationFrame(updateParallax);
                    ticking = true;
                }
            }

            window.addEventListener('scroll', requestTick, { passive: true });
            // Initialize on load
            updateParallax();
        })();
    </script>

    <script src="/vendor/cookieconsent.umd.js"></script>
    <script src="/js/cookieconsent-config.js"></script>
</body>
</html>
